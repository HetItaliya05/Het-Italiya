# Vercel frontend env
# VITE_ vars must be prefixed with VITE_

# Backend base URL (no /api suffix)
# Example: https://your-backend.onrender.com
VITE_API_URL=

# Dev-only: allow Vite dev server to proxy /api and /uploads
VITE_API_PROXY_TARGET=http://localhost:5000

